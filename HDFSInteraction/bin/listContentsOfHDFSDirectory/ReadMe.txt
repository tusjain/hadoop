Q) How to list out the files and sub directories in the specified directory in HDFS using java program?

TListDirectoryContents prints the contents (files and directories) of a given directory(/user/hadoop) in HDFS.

hdfs://localhost:54310 is the namenode URI. 